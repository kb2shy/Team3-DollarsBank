import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: "#599532",

        },
        secondary: {
            main: "#2B5A0C"
        }
    },
    background: {
        default: "#fff"
    }
})

export default theme;